diff -Naur a/etc/ansible/roles/os_heat/tasks/heat_install.yml b/etc/ansible/roles/os_heat/tasks/heat_install.yml
--- a/etc/ansible/roles/os_heat/tasks/heat_install.yml	2017-05-29 05:34:44.473715468 -0400
+++ b/etc/ansible/roles/os_heat/tasks/heat_install.yml	2017-05-29 05:27:41.501707905 -0400
@@ -39,11 +39,18 @@
   retries: 5
   delay: 2
 
+- name: Retrieve checksum for venv download
+  uri:
+    url: "{{ heat_venv_download_url | replace('tgz', 'checksum') }}"
+    return_content: yes
+  register: heat_venv_checksum
+  when: not heat_developer_mode | bool
+
 - name: Attempt venv download
   get_url:
     url: "{{ heat_venv_download_url }}"
     dest: "/var/cache/{{ heat_venv_download_url | basename }}"
-    checksum: "sha1:{{ lookup('url', heat_venv_download_url | replace('tgz', 'checksum')) }}"
+    checksum: "sha1:{{ heat_venv_checksum.content | trim }}"
   register: heat_get_venv
   when: not heat_developer_mode | bool
 
