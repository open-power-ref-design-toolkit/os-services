diff -Naur a/etc/ansible/roles/os_horizon/tasks/horizon_install.yml b/etc/ansible/roles/os_horizon/tasks/horizon_install.yml
--- a/etc/ansible/roles/os_horizon/tasks/horizon_install.yml	2017-05-29 05:34:50.305715572 -0400
+++ b/etc/ansible/roles/os_horizon/tasks/horizon_install.yml	2017-05-29 05:29:22.009709702 -0400
@@ -39,11 +39,18 @@
   retries: 5
   delay: 2
 
+- name: Retrieve checksum for venv download
+  uri:
+    url: "{{ horizon_venv_download_url | replace('tgz', 'checksum') }}"
+    return_content: yes
+  register: horizon_venv_checksum
+  when: not horizon_developer_mode | bool
+
 - name: Attempt venv download
   get_url:
     url: "{{ horizon_venv_download_url }}"
     dest: "/var/cache/{{ horizon_venv_download_url | basename }}"
-    checksum: "sha1:{{ lookup('url', horizon_venv_download_url | replace('tgz', 'checksum')) }}"
+    checksum: "sha1:{{ horizon_venv_checksum.content | trim }}"
   register: horizon_get_venv
   retries: 5
   delay: 5
