diff -Naur a/etc/ansible/roles/os_neutron/tasks/neutron_install.yml b/etc/ansible/roles/os_neutron/tasks/neutron_install.yml
--- a/etc/ansible/roles/os_neutron/tasks/neutron_install.yml	2017-05-29 05:34:48.473715539 -0400
+++ b/etc/ansible/roles/os_neutron/tasks/neutron_install.yml	2017-05-29 05:30:26.009710846 -0400
@@ -120,11 +120,18 @@
   retries: 5
   delay: 2
 
+- name: Retrieve checksum for venv download
+  uri:
+    url: "{{ neutron_venv_download_url | replace('tgz', 'checksum') }}"
+    return_content: yes
+  register: neutron_venv_checksum
+  when: not neutron_developer_mode | bool
+
 - name: Attempt venv download
   get_url:
     url: "{{ neutron_venv_download_url }}"
     dest: "/var/cache/{{ neutron_venv_download_url | basename }}"
-    checksum: "sha1:{{ lookup('url', neutron_venv_download_url | replace('tgz', 'checksum')) }}"
+    checksum: "sha1:{{ neutron_venv_checksum.content | trim }}"
   register: neutron_get_venv
   when: not neutron_developer_mode | bool
 
