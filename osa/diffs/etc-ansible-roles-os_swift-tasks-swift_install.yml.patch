diff -Naur a/etc/ansible/roles/os_swift/tasks/swift_install.yml b/etc/ansible/roles/os_swift/tasks/swift_install.yml
--- a/etc/ansible/roles/os_swift/tasks/swift_install.yml	2017-05-29 05:34:42.073715425 -0400
+++ b/etc/ansible/roles/os_swift/tasks/swift_install.yml	2017-05-29 05:33:25.301714052 -0400
@@ -39,11 +39,20 @@
   retries: 5
   delay: 2
 
+- name: Retrieve checksum for venv download
+  uri:
+    url: "{{ swift_venv_download_url | replace('tgz', 'checksum') }}"
+    return_content: yes
+  register: swift_venv_checksum
+  when:
+    - not swift_pypy_enabled | bool
+    - not swift_developer_mode | bool
+
 - name: Attempt venv download
   get_url:
     url: "{{ swift_venv_download_url }}"
     dest: "/var/cache/{{ swift_venv_download_url | basename }}"
-    checksum: "sha1:{{ lookup('url', swift_venv_download_url | replace('tgz', 'checksum')) }}"
+    checksum: "sha1:{{ swift_venv_checksum.content | trim }}"
   register: swift_get_venv
   when:
     - not swift_pypy_enabled | bool
