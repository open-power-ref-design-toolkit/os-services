diff -Naur a/opt/openstack-ansible/playbooks/os-swift-install.yml b/opt/openstack-ansible/playbooks/os-swift-install.yml
--- a/opt/openstack-ansible/playbooks/os-swift-install.yml	2017-05-31 05:12:47.788781828 -0400
+++ b/opt/openstack-ansible/playbooks/os-swift-install.yml	2017-05-31 03:31:55.376673603 -0400
@@ -54,6 +54,7 @@
   vars:
     is_metal: "{{ properties.is_metal|default(false) }}"
     pip_lock_to_internal_repo: "{{ (pip_links | length) >= 1 }}"
+  environment: "{{ deployment_environment_variables | default({}) }}"
   tags:
     - swift
 
@@ -73,5 +74,6 @@
     - role: "system_crontab_coordination"
       tags:
         - crontab
+  environment: "{{ deployment_environment_variables | default({}) }}"
   tags:
     - swift
