diff -Naur a/opt/openstack-ansible/playbooks/repo-install.yml b/opt/openstack-ansible/playbooks/repo-install.yml
--- a/opt/openstack-ansible/playbooks/repo-install.yml	2016-06-16 07:24:37.970584481 -0400
+++ b/opt/openstack-ansible/playbooks/repo-install.yml	2016-06-16 07:25:48.450456216 -0400
@@ -13,5 +13,18 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+- name: Build KVM on Power repo
+  hosts: localhost
+  connection: local
+
+  tasks:
+  - name: Build repo
+    command: /usr/local/bin/openstack-ansible -vvvv pkvm-repo-build.yml chdir=/opt/openstack-ansible/playbooks
+    register: command_result
+    failed_when: "'FAILED' in command_result.stderr"
+    when: hostvars[groups['compute_all'][0]]['ansible_architecture'] == 'ppc64le'
+
 - include: repo-server.yml
 - include: repo-build.yml
+- include: pkvm-repo-post-build.yml
+  when: hostvars[groups['compute_all'][0]]['ansible_architecture'] == 'ppc64le'
